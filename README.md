# RO-Crate to Dataverse JSON Converter

A modular Python tool to convert ARC RO-Crates and Schema.org JSON-LD to Dataverse JSON (FAIRagro spec).

## Usage

```bash
uv run python main.py <input_path> --type <rocrate|schemaorg>
```
